# Native Salt modules for Foreman integration

Replace all the smart_proxy_salt features like foreman-node with
Salt native modules

Written in a way so it is also cluster aware and works when running
a Salt cluster

Will be packaged as Salt extension later for easy installation.
